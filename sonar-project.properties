sonar.projectKey=suchika
sonar.projectName=Suchika
sonar.projectVersion=1.0
sonar.sourceEncoding=UTF-8

# ---------- Server ----------
sonar.host.url=http://localhost:9000
# Token from http://localhost:9000/account/security — kept here because this file is gitignored
sonar.token=sqp_67feaac2593aeeda89632d4dbbdc5f828c1c5437

# ---------- Sources ----------
# Java backend + React frontend (excludes test files)
sonar.sources=application,shared,web/src

# Test source directories — Java unit tests + React Jest tests + Playwright E2E
sonar.tests=application,shared,web/src,web/e2e
sonar.test.inclusions=\
  **/src/test/**,\
  **/*.test.js,\
  **/*.spec.js,\
  **/setupTests.js

# ---------- Java binaries ----------
# Ant-style glob -- matches every Gradle sub-module's compiled output automatically.
# No need to list individual paths; adding a new domain module just works.
sonar.java.binaries=**/build/classes/java/main
sonar.java.test.binaries=**/build/classes/java/test

# ---------- Java libraries ----------
# Gradle cache -- gives Sonar the dependency JARs so it can resolve types precisely.
# Uses GRADLE_USER_HOME env var (defaults to ~/.gradle on all platforms).
sonar.java.libraries=C:/Users/91779/.gradle/caches/modules-2/files-2.1/**/*.jar
sonar.java.test.libraries=C:/Users/91779/.gradle/caches/modules-2/files-2.1/**/*.jar

# ---------- Exclusions ----------
sonar.exclusions=\
  **/build/**,\
  **/node_modules/**,\
  **/generated.ts,\
  **/flyway/**,\
  **/*.md,\
  **/*.yaml,\
  **/*.yml,\
  **/*.sql,\
  web/src/reportWebVitals.js

sonar.coverage.exclusions=\
  **/adapters/http/dto/**,\
  **/*Application.java,\
  **/domain/**,\
  web/src/index.js,\
  web/src/setupTests.js,\
  web/src/reportWebVitals.js

# ---------- Test coverage (JaCoCo — Java) ----------
# Uncomment after adding jacoco plugin to build.gradle.kts and running:
#   ./gradlew test jacocoTestReport
# sonar.coverage.jacoco.xmlReportPaths=**/build/reports/jacoco/test/jacocoTestReport.xml

# ---------- Test coverage (LCOV — JavaScript/React) ----------
# Generated by: cd web && npm run test:coverage
sonar.javascript.lcov.reportPaths=web/coverage/lcov.info

# ---------- Language hints ----------
sonar.java.source=17
